# Starcat 功能复盘（第 1 期）

> **复盘时间**：2026-09-16
> **复盘基准**：`dev` 分支 @ `4e48adf7`（v1.7.0 发布之后的第一个提交）
> **当前版本**：**v1.7.0**（2026-09-16 发布完成：Direct 公证 / 官网 / appcast / Homebrew 完结，App Store 已上传；git tag `v1.0.0` ~ `v1.7.0` 共 9 个正式版本）
> **复盘方法**：以代码为准。五路核对：`Starcat/Features`（25 模块 506 文件）、`Starcat/Core`（24 模块）、数据库迁移账本（23 条正式迁移）、`StarcatTests`（333 个测试文件）、`supports/`（29 个配套仓库）；再对照 `docs/功能实现总览.md`、78 份详细设计、需求讨论与 4-工程进度专项文档。
> **性质**：只读复盘。本文档不改动 `docs/功能实现总览.md` 与任何源码。

---

## 0. 一页结论

1. **代码面远比文档面新**。总览的 checkbox 系统性更新大约停在 **2026-08 下旬**；1.6.0～1.7.0 期间落地的功能大量只存在于 changelog 与 `docs/4-工程进度/` 专项目录中，**没有进入总览 checkbox**。总览自报 165/193（85%）已严重偏低。
2. **已实现但总览未登记的功能级条目 ≥ 15 项**（详见 §3），最大的几项：**macOS 桌面小组件（双渠道 WidgetKit 扩展）**、**本地 AI（进程内 MLX）**、**Anthropic + CC-Switch 导入**、**AI 用量统计面板**、**AI 个人笔记生成**、**Awesome 发现来源体系**、**智能集合 Smart Collections**、**Alfred / Raycast / uTools 三端外部集成**、**Liquid Glass 适配**。
3. **只有方案、没有落地的需求 ≥ 12 项**（详见 §4），最近的三个：**仓库社区动态 Feed**（2026-08-30，待审阅实施）、**AI 流式正文卡顿优化**（2026-08-19，待开干）、**AgentRuntime 与 AI 服务统一路由**（2026-08-25，待开干授权）。
4. **文档存在反向脏数据**：已登记完成的「精选发布台」代码已于 2026-09-06 整体移除；1438/1439 行有同条目一未勾一已勾的重复；上架准备清单多项未勾但 App 已发布 7 个版本。
5. **代码质量信号健康**：Features 全量 506 个 Swift 文件仅 1 处 TODO（`TrendingViewModel.swift:734`），无 FIXME、无空壳 View；唯一 Release 无入口的模块是 Ambient（有意 DEBUG-only 实验特性）。

---

## 1. 基础信息快照

| 维度 | 数据 |
|---|---|
| 产品 | Starcat — macOS GitHub Star 管理与 AI 知识整理工具（SwiftUI + macOS 15+，Swift 6 complete concurrency） |
| 分发渠道 | App Store（`com.starcat.app.store`，StoreKit 2）+ Direct（`starcat.ink`，Developer ID 公证 DMG + Sparkle + Homebrew + Creem License） |
| 主程序结构 | 5 个 Scene：主窗口（三栏）、设置窗、RAG 工作台窗、Agent 工作台窗、LocalAI 日志窗；菜单栏常驻 `StatusBarController` |
| 主窗口导航 | Sidebar 四大根页面：**星标（Manage）/ 探索（Explore）/ 动态（Activity）/ 洞察（Insights）** + 我的项目 / 未分类 / 知识库 / 智能集合 / Release 时间线 / GitHub Star Lists / 标签墙 / 语言分组 |
| Explore 模式 | 6 个：discover / trending / popular / newReleases / weekly / **awesome** |
| 设置页 | 原生导航式，**19 个 tab**（账户组 5 + AI 组 5 + RAG 组 3 + 扩展组 4 + 支持组 2） |
| 数据库 | GRDB，**23 条正式迁移**（v1-initial → v23-public-repo-star-history；开发期标识按版本 merging 收口，如 v19-release-1.4.0 / v20-release-1.5.0） |
| 本地化 | **18 种语言**，5839 个 key |
| 测试 | `StarcatTests` **333 个测试文件**（总览口径 2288 test items / 0 failures，基线 2026-08-18，1.7.0 期间又新增多轮未计入） |
| 配套生态 | `supports/` 下 **29 个配套仓库**（12 个 Go API / 2 个 Python 数据服务 / CLI / Skill / 本地化 / 双浏览器插件 / Alfred·Raycast·uTools / 2 个 Homebrew tap / 官网 / 文档站 / admin-console 等） |
| 工作区状态 | `dev` 分支干净提交；工作树仅剩 `skills/` 目录整体删除（迁移至他处的遗留，与 App 功能无关） |

---

## 2. 已完成功能清单（按产品域，代码已核实）

> 状态符号：✅ 已完成且总览已登记 ｜ 🧩 已实现但总览无 checkbox（详见 §3）｜ ⏸ 部分完成 ｜ 🧪 实验/DEBUG 特性

### 2.1 账号与同步
| 功能 | 状态 | 代码证据 |
|---|---|---|
| GitHub OAuth 三通道（Device / Web PKCE / PAT）+ AuthSession | ✅ | `Features/Auth/`（AuthSession、GithubWebFlowService） |
| 多账号数据隔离（DB 按账号隔离） | ✅ | `SwitchUserDatabaseTests` |
| GitHub App 私有仓库授权（我的项目私仓） | ✅ | `Features/Auth/ProjectAccessSession.swift` + v17-my-projects |
| Stars 全量 / 增量同步、Rate Limit 退避、ETag 缓存 | ✅ | `Core/Sync/SyncManager.swift` |
| Token AES-GCM 加密存储（Keychain） | ✅ | `Core/Keychain/`（D-16 收口） |

### 2.2 整理（核心价值「整理」）
| 功能 | 状态 | 代码证据 |
|---|---|---|
| 标签 CRUD / 颜色 / SF Symbol、批量打标 | ✅ | `Features/Tags/` |
| 私有笔记 + 状态管理（防抖自动保存） | ✅ | `Features/Notes/RepoNotesSection.swift` |
| **AI 个人笔记生成**（按仓库上下文起草笔记） | 🧩 | `Features/Notes/RepoNoteAIGeneration*`（3 文件）+ 专项 6 份审查报告；设计 47 |
| GitHub Stars List 分组 + 多选管理 + AI 仓库分组 | ✅ | `Features/AI/GitHubStarListAIGroupingSession.swift` + v22 系列 |
| 仓库置顶 Pin（v15 repo_pins） | ✅ | `Core/Database` + `RepoListView` |
| **智能集合 Smart Collections**（系统 + 用户规则、规则编辑器、瀑布流总览） | 🧩 | `Features/Collections/`（10+ 文件）+ Sidebar `smartCollectionsHome` + 7 个 Smart* 测试；有正式方案无 checkbox |
| 自动后台 AI 整理 + 批量整理工作台（自动建标 / 批量选择 / 阈值预选） | ✅ | `Features/AI/BatchAIWorkspaceView.swift` + `AutoTidySettings` |

### 2.3 详情页与 README
| 功能 | 状态 | 代码证据 |
|---|---|---|
| README WebView 渲染 + SWR + ETag + 软过期（三阶段缓存） | ✅ | `Shared/Components/ReadmeWebView.swift` + `ReadmeViewModel.swift` |
| README 视频 / Mermaid / 图片预览 / 页内搜索 ⌘F / 阅读工具条 | ✅ | 同上 + `ReadmeAssetURLRewriter.swift` |
| **独立 README 窗口**（`onOpenInNewWindow` → ReadmeWindowController） | 🧩 | `Features/Detail/ReadmeWindowController.swift`，`RepoDetailView.swift:768` 调起；总览 P0/P2 两条均未勾 |
| README 后台预拉 + 首次预热（README / OpenSSF / Health） | ✅ | `Core/Sync/ReadmePrefetchService.swift` + `Core/Warmup/` |
| README 分段 / 全文翻译、4 路并发、切仓守门、源语言投票 | ✅ | `Features/AI/ReadmeTranslationService.swift` + `TranslationSourceLanguageGate` |
| **翻译服务设置页**（系统翻译 / Google 翻译引擎、语言包管理） | 🧩 | `Features/Settings/TranslationSettingsView.swift` + `SystemTranslationLanguage*`；1.7.0 |
| **仓库 Owner 资料卡与关注（Follow）** | 🧩 | `Features/Profile/OwnerCardView.swift`；1.6.0 / 2026-09-05 |
| **贡献者行**（三头像重叠 + 溢出计数 + 全列表） | 🧩 | `Core/Insights/RepositoryContributorHeroService.swift`；1.7.0 |
| **Own Forks 分组 / 上游 / ahead-behind 一键同步** | 🧩 | `Features/Home/ForksMenu.swift`；1.7.0 |
| Clone URL 复制、外部链接菜单、Star/Unstar 即时 ±1 | ✅ | `StarringSubsystem.swift` |

### 2.4 搜索
| 功能 | 状态 | 代码证据 |
|---|---|---|
| FTS5 全文检索 + BM25 + 安全化 | ✅ | `Core/Database` |
| 全局搜索中心（本地关键词 / 语义 / GitHub / Web 四源合并） | ✅ | `Features/Search/SearchCenterView.swift` + `RepositorySearchMerger` |
| 语义搜索（Embedding 本地存储、三级降级、覆盖率进度环、README 纳入召回） | ✅ | `Features/AI/SemanticSearchService.swift` + `SmartSearchField.swift` |
| 外部搜索 Provider 化（AnySearch / Tavily / Exa / Brave / **Firecrawl**） | ✅（Firecrawl 为 changelog-only） | `Features/ExternalSearch/` |
| **Alfred / Raycast / uTools 外部搜索集成** | 🧩 | `supports/starcat-alfred-workflow`（Go）/ `starcat-raycast-extension`（TS）/ `starcat-utools-plugin`（JS）+ `GlobalRepositorySearchService.swift`（AppDependencies:1236 注入）；设计 52/53/54 + ADR-0001 |
| Spotlight 索引与三栏定位（App Intents + Core Spotlight） | ✅ | `Core/Spotlight/`（默认关闭、显式授权） |

### 2.5 发现 / 推荐 / Trending
| 功能 | 状态 | 代码证据 |
|---|---|---|
| GitHub Trending（日/周/月榜）+ 持久化 + AI 摘要 | ✅ | `Features/Trending/`（全模块唯一 TODO 在 `TrendingViewModel.swift:734`） |
| 探索页（发现 / 热门 / 新发布，bulk 本地优先缓存） | ✅ | `Features/Explore/` + `starcat-discovery-api` |
| Weekly 多来源（阮一峰 / ZRead / Show HN / HelloGitHub / AI 情报 + 置顶） | ✅ | `starcat-weekly-api` + v13；本地运营台 `_local-admin` |
| **Awesome 发现来源体系**（Awesome README 解析、来源管理、自定义来源、Sidebar 来源分组） | 🧩 | `Core/Sync/AwesomeReadmeParser.swift` / `AwesomeCustomSourceService.swift` / `AwesomeRepository.swift` + Explore awesome 模式（`AwesomeStore.swift`、`AwesomeSourceManagerSheet.swift`）+ v22-awesome-discovery + 5 个 Awesome* 测试 + 专项结果报告。注意：与未做的「Awesome List 导出」是两回事 |
| 相似仓库推荐（App Store v1 SimRepo / Direct 自研 v2）+ 匿名数据贡献 | ✅ | `Core/DataContribution/` + `starcat-collection-api` / `starcat-recsys-trainer` / `starcat-recommend-api` |
| **自研星标历史服务**（history-api + admin-console，README 星史卡片接入） | 🧩（部分） | `supports/starcat-history-api`（1443 文件，2026-09-15 活跃）+ `starcat-admin-console`；客户端 `RepoStarHistoryRepository` / `ReadmeStarHistoryHTMLRenderer`；v21 切 GitHub 官方唯一源 / v23 公开仓星史缓存 |

### 2.6 活动与通知
| 功能 | 状态 | 代码证据 |
|---|---|---|
| Activity 聚合页（关注 Events / 公告 RSS / Security Advisory / 撤销 Star） | ✅ | `Features/Activity/` |
| GitHub 通知时间线（inbox、Issue/PR 状态、入库标记、筛选、后台系统通知） | ✅ | `GitHubNotificationInbox*` + v19-release-1.4.0 系列 |
| 通知会话增强（翻译 / AI 撰写评论 / 草稿保留 / 粘贴图片 / 评论菜单 / Issue 事件流缓存） | ✅ | `GitHubNotificationDetailView.swift` 等全链路 |

### 2.7 Release 订阅
| 功能 | 状态 | 代码证据 |
|---|---|---|
| 订阅 / 轮询 / 通知 / 时间线 / 已读管理 / 智能资产过滤 / 下载 / Markdown 截图适配 | ✅ | `Features/Releases/` + `Core/Scheduling/ReleasePoller.swift` + `Core/Download/` |

### 2.8 AI 基座
| 功能 | 状态 | 代码证据 |
|---|---|---|
| 多 Provider：OpenAI-compatible / DeepSeek / OpenRouter / **OrcaRouter**（changelog-only）/ **Anthropic** 🧩 | ✅ / 🧩 | `Features/AI/AIClient.swift`（`.anthropic` → `AnthropicClient` Messages API）+ Anthropic 测试 + 专项 + 设计 68 |
| **本地 AI：进程内 MLX（LocalMLXClient）**，模型按需下载（HuggingFace / ModelScope）、本地 Reranker、运行日志窗 | 🧩 | `Features/AI/LocalMLXRuntime.swift` + `Settings/LocalAIModelsSection.swift` + StarcatApp LocalAI 日志 scene + **15 个 LocalAI* 测试**；方案 v1.1（2026-09-12）；1.7.0 主打特性 |
| **CC-Switch 一键导入 Provider 配置** | 🧩 | `Features/Settings/CCSwitchImport/`（extractor / preview sheet / POSIXHome）+ 测试；设计 69 |
| **AI 用量统计面板**（Dashboard / Recorder / Repository / Statistics / Pricing / 独立窗） | 🧩 | `Features/AI/Usage/`（7 文件）+ v14-ai-usage-events 迁移 + 专项 4 份审查 + 结果报告；设计 37 |
| AI 摘要 / 标签推荐 / 确认流 / 缓存 / 对话多 session / 流式 Think | ✅ | `Features/AI/`（88 文件） |
| RepoContextPacker（自研 Repomix 替代）、CodeFlow、CodebaseMemory 集成 | ✅ | `Features/CodeGraph/`（App Store 内置 0.8.1 / Direct 外部检测） |

### 2.9 知识库 RAG（独立工作台窗口）
| 功能 | 状态 | 代码证据 |
|---|---|---|
| RAG 工作台整体（会话 / 检索漏斗 / 引用 / Debug Trace / @ 多选 / 分组置顶 / 联网审计 / 结构化分析 DSL） | ⏸ | `Features/RAG/`（73 文件）+ v7/v8/v9~v12/v18 迁移。总览自述：金标评测与 UI 人工验收待做 |
| RAG 推理后端（BYOK 模型 / **Codex CLI / Claude Code CLI**（Direct）/ 可选 Rerank / Meilisearch / Qdrant） | ✅ | `RAGCLIModelClient.swift` + `RAGSearchProviders.swift` + `docker/meilisearch` `docker/qdrant` |
| 知识库浏览器（分片管理 / RepoContext XML / 召回测试调参） | ✅ | `KnowledgeRAGWorkspaceWindowController.swift` |

### 2.10 Agent 工作台（独立窗口）
| 功能 | 状态 | 代码证据 |
|---|---|---|
| 内置 Loop Runtime + 4 个 Agent（Weekly 周报 / Repo Insight / Repo Alternatives / Untagged Tidy）+ 写入审批 + Run 持久化历史 | ✅ | `Features/Agents/`（v3/v4/v19 迁移） |
| 外部 Runtime：Codex App Server + DeepSeek Harness（Direct 渠道门控） | ⏸ | `Features/Agents/ExternalRuntime/`（总览自述「基本可用，仍需打磨」） |

### 2.11 生态集成与对外服务
| 功能 | 状态 | 代码证据 |
|---|---|---|
| MCP 本地 Server（loopback TLS + 配对审批 + 工具注册 + 审计） | ✅ | `Features/MCP/`（11 文件） |
| 浏览器伴侣（Chrome / Safari 插件：标签、AI Tab、Open in Starcat、Health） | ✅ | `Features/Companion/`（本机 HTTP 服务）+ `supports/extensions/` |
| 统一本地 API Key（MCP / 插件共用） | ✅ | `StarcatLocalAPIKeyStore.swift` |
| 菜单栏常驻入口（左键开窗 / 右键菜单 / 隐藏 Dock） | ✅ | `App/StatusBarController.swift` |
| **macOS 桌面小组件（WidgetKit，双渠道两个扩展 target）** | 🧩 | `Core/Widget/`（快照构建 / 刷新协调 / 发布门控）+ `StarcatWidgets/` 源码 + project.yml `StarcatWidgets` & `StarcatDirectWidgets` target + **8 个 Widget* 测试套件** + 专项 9 轮审查报告；总览 1539 行仍 `- [ ] Widget P2`；设计 55/56 |
| Starcat CLI + Skill | ✅ | `supports/starcat-cli`（Go）+ `supports/starcat-skill` |
| App Store / Direct 双通道版本检测升级 | ✅ | `Core/Updates/`（Sparkle + Lookup API） |

### 2.12 洞察
| 功能 | 状态 | 代码证据 |
|---|---|---|
| 我的洞察（全部收藏 / 知识库双范围、可下钻待办） | ✅ | `Features/Insights/MyInsights*` |
| 仓库洞察（活动 / 提交 / 贡献者 / 发布节奏 / 安全公告 / Release 附件） | ✅ | `RepositoryInsights*` + v16 迁移 |
| README Star History / Star Journey（全历史趋势、里程碑时间线） | ✅ | `ReadmeStarHistory*`（HTML 渲染 + DOM 注入）+ v23 迁移 |
| 项目健康度评分 + OpenSSF Scorecard（雷达图） | ✅ | `Core/Health/` + `Core/Sync/OpenSSF*` |

### 2.13 支付 / 订阅 / 分发
| 功能 | 状态 | 代码证据 |
|---|---|---|
| StoreKit 2（月订 + 年订 + 恢复）+ Pro 门控 + 付费墙 | ✅ | `Core/Subscription/` |
| Direct：Creem checkout + License API（激活 / 静默校验 / 设备管理 / webhook 幂等 / 月订阅取消） | ✅ | `DirectLicenseAPI.swift` / `DirectLicenseManager.swift` + `starcat-license-api` |
| 渠道能力门控（DistributionGate：Direct-only 功能统一入口） | ✅ | `Core/Distribution/` |
| 公开仓库分享链接与 OG 预览 | ⏸ | `RepositoryDeepLink` + `starcat-sharing-api`；总览自述：生产部署与跨平台人工验收待完成 |
| 发布流水线（双渠道签名 / 公证 / appcast / Homebrew / 断点续跑 / 远程校验） | ✅ | `scripts/package-*` `release-*` + Makefile 33 target + 6-发版与上架 SOP 体系 |

### 2.14 其他
| 功能 | 状态 | 代码证据 |
|---|---|---|
| **Liquid Glass（macOS 26）适配** | 🧩 | 1.6.1 changelog 明确「主窗口、搜索、仓库详情、AI 与知识库工作台应用系统 Liquid Glass」；总览 P0（1157 行）与非功能（1925 行）仍未勾 |
| **设置页大改版**（原生 Sidebar 导航 19 tab、通知 / 快捷键独立成页、隐私页） | 🧩（隐私页 1.6.0、重组 1.7.0） | `Features/Settings/SettingsView.swift:107-205` |
| **Ambient 沉浸图片墙**（repos / owners 场景） | 🧪 | `Features/Ambient/`（App + Core）+ 6 个 Ambient* 测试；仅 DEBUG 菜单入口（`StarcatApp.swift:837` 在 `#if DEBUG` 内），Release 不可达 |
| **Lab 实验窗口 / BorderBeamKit 搜索条** | 🧪 | `Shared/Components/BorderBeamSearchField.swift`（实验已沉淀为共享组件） |
| 首启引导 + 上手任务清单 + 数据贡献授权 sheet | ✅ | `Features/Onboarding/` |
| 18 语言本地化 | ✅ | `Localizable.xcstrings`（18 locale / 5839 key）+ `starcat-localization` 仓库 |
| 遥测（OSSignposter + MetricKit + Aptabase opt-in）与诊断边界 | ✅ | `Shared/Telemetry` / `Shared/Diagnostics` |

> 另有一批 1.6–1.7 打磨项只有 changelog 记录、无 checkbox（星标徽章实心/空心、语言菜单原生名、语义搜索底栏过程与匹配度、缓存用量总览、OAuth 权限披露、智能集合行内创建、探索懒加载骨架屏、详情工具栏克隆菜单合并等），属改进级，不单列。

---

## 3. 差异 A：代码已实现、总览未登记（文档落后于代码）

> 这是本次复盘最重要的产出。以下条目在代码、测试、专项文档、changelog 中均有实据，但 `docs/功能实现总览.md` 中**没有对应 checkbox**（或仍是未勾选状态）。

| # | 功能 | 代码 / 测试证据 | 旁证文档 | 落地时间（约） |
|---|---|---|---|---|
| A1 | **macOS 桌面小组件（WidgetKit）** | `Core/Widget/`、`StarcatWidgets/`、project.yml 双 target、8 个测试套件 | `4-工程进度/macOS桌面小组件专项`（9 轮审查）；设计 55/56 | 1.6.x 周期 |
| A2 | **本地 AI（进程内 MLX）** | `LocalMLXRuntime/Client`、LocalAI 日志窗 scene、15 个 LocalAI* 测试 | 需求讨论《starcat-local-ai-framework-and-model-plan》v1.1；1.7.0 changelog | 2026-09（1.7.0） |
| A3 | **Anthropic 服务商（Messages API）** | `AIClient.swift` `.anthropic` 分支 + 测试 | `4-工程进度/Anthropic与CCSwitch导入专项`；设计 68 | 2026-09（1.7.0） |
| A4 | **CC-Switch 导入 Provider** | `Settings/CCSwitchImport/` 3 文件 + 测试 | 同上专项；设计 69 | 2026-09（1.7.0） |
| A5 | **AI 用量统计面板** | `Features/AI/Usage/` 7 文件 + v14 迁移 | `4-工程进度/AI用量统计面板专项`（4 审查 + 结果报告）；设计 37 | 1.5.x 周期 |
| A6 | **AI 个人笔记生成** | `Features/Notes/RepoNoteAIGeneration*` | `4-工程进度/AI个人笔记生成专项`（6 审查）；设计 47 + 正式方案 | 1.5.x 周期 |
| A7 | **Awesome 发现来源体系** | `Core/Sync/Awesome*` 4 文件 + Explore awesome 模式 + v22 迁移 + 5 测试 | 《Awesome发现栏目与来源管理正式方案》+ 专项结果报告 | 1.5.x～1.6.x |
| A8 | **智能集合 Smart Collections** | `Features/Collections/` 10+ 文件 + Sidebar 入口 + 7 测试 | 《Smart Collections 智能集合方案》 | 1.4.x 起，持续演进 |
| A9 | **Alfred / Raycast / uTools 集成** | 3 个独立配套仓 + `GlobalRepositorySearchService` 注入 | 设计 52/53/54 + ADR-0001 | 2026-08 |
| A10 | **Liquid Glass 适配** | 1.6.1 changelog 明确已应用 | 总览 P0/非功能两条仍未勾 | 1.6.1 |
| A11 | **独立 README 窗口** | `ReadmeWindowController` + 详情页调起 | 总览 P0 1202 / P2 1780 两条未勾 | ≤ 2026-08-22 |
| A12 | **翻译服务设置页 + 系统语言包管理** | `TranslationSettingsView` + `SystemTranslationLanguage*` | changelog 2026-09-12 ×3；1.7.0 | 1.7.0 |
| A13 | **设置页重组 / 隐私设置页** | `SettingsView` 19 tab 导航 | changelog 2026-09-01 / 1.6.0 / 1.7.0 | 1.6.0～1.7.0 |
| A14 | **Owner 资料卡与关注** | `Profile/OwnerCardView.swift` | changelog 2026-09-05 / 1.6.0 | 1.6.0 起 |
| A15 | **贡献者行 / 贡献者样本** | `Core/Insights/RepositoryContributorHeroService.swift` | 1.7.0 changelog | 1.7.0 |
| A16 | **Own Forks 分组与一键同步** | `Features/Home/ForksMenu.swift` | 1.7.0 changelog | 1.7.0 |
| A17 | **Ambient 沉浸图片墙（DEBUG）** | `Features/Ambient/` + 6 测试 | changelog 2026-09-05 | 2026-09-05 |
| A18 | **自研星标历史服务 + admin-console（部分落地）** | `starcat-history-api`（2026-09-15 活跃）、`starcat-admin-console`、客户端星史卡片接入、v21/v23 迁移 | 设计 62/66/67 + 运维指南 / 测试报告 | 2026-08 起，进行中 |
| A19 | **OrcaRouter 预设 / Firecrawl 搜索源 / 搜索匿名模式** | provider 注册表扩展 | changelog 2026-09-05 / 2026-09-02 | 1.6.0～1.7.0 |

> 按铁律 #4，以上条目**未写入总览**。如需补登记，建议按 4 条协作规范逐条补 `- [x]` + `> 实现：` 行，并同步重算仪表盘。

---

## 4. 差异 B：只有方案 / 需求讨论、尚未落地

> 代码侧核实均无对应实现（或仅有数据库预留骨架）。

| # | 需求 / 方案 | 文档位置与自述状态 | 代码核实 |
|---|---|---|---|
| B1 | **仓库社区动态 Feed** | `需求讨论/仓库社区动态Feed需求讨论.md`（2026-08-30：「待 dong4j 审阅后实施」）+ 正式方案 | 无代码 |
| B2 | **AI 流式正文卡顿优化**（主窗口 AI 正文 liveTail） | `需求讨论/AI流式正文卡顿优化方案.md`（2026-08-19：「待确认开干，未改代码」） | RAG 侧已优化，主窗口正文未动 |
| B3 | **AgentRuntime 与 AI 服务统一路由** | 详细设计 65（2026-08-25：「方案已认可，等待开干授权，未开始实施」） | 无代码（现有 Runtime 设置属 59 号 POC） |
| B4 | **ACP 协议接入** | 详细设计 64：明确**暂缓** | 无代码（有意） |
| B5 | **详情页 Hero 信息增强 4 项**（Website 内联 / Open Issues / Open PR / 最新 Release） | v2 规划 §1（2026-06-17 延期至 v2，布局方案已定型） | `RepoMetadataHeaderView` 无 homepage / openIssues 内联展示 |
| B6 | **导出 Starred 到 GitHub Awesome 仓库**（配置 / README 生成 / 推送 / 定时） | v2 规划 §4（参考 mawesome） | 无代码（≠ 已实现的 Awesome 发现来源） |
| B7 | **Trending 推荐栏目**（GitHub Issues 原生详情） | v2 规划 §5（4 个待拍板决策） | 无代码 |
| B8 | **JSON 导入 / 导出**（OhMyStar / Astral 兼容） | P0 遗留，2026-05-31 起多次延后 | 无 `Features/DataIO` |
| B9 | **CloudKit 多端同步**（schema / 推送 / 拉取合并 / 冲突 UI，原 Week 5） | P2 整块 + 《CloudKit数据同步设计》正式方案 | 无 CloudKit 实现代码 |
| B10 | **AI 代理 API（自建服务模式）** | 《AI代理API设计》正式方案 | 未实施；实际路线已转为 BYOK + 本地 MLX，原方案可视为路线变更 |
| B11 | **Companion V0.2 / V0.3**（插件内 Issue/PR/Release AI 总结；HN/Reddit/博客跨站采集） | P2 浏览器伴侣章节 | 无代码（V0.1 条目未勾但核心能力已由 2026-07-02 系列实现覆盖，条目口径过时） |
| B12 | **Agent 讨论池其余候选**（agents-radar 公共雷达、回忆搜索、Release 影响、重叠扫描、Unread 激活、技术栈迁移、项目采用计划等） | `需求讨论/agent/` 00–21 号讨论 | 无代码（03 周报 / 13 Untagged 整理等已落地为内置 Agent） |

---

## 5. 明确未做 / 长期搁置的原规划项（文档与代码一致）

| 类别 | 条目 | 备注 |
|---|---|---|
| 多端 | iPhone / iPad / watchOS 适配 | 工程仍 macOS-only |
| Apple 生态 | 全局快捷键（全局唤起） | 应用内快捷键可配置（`ShortcutRecorderView` / `KeyboardShortcutConfiguration`），全局热键无 |
| Apple 生态 | Siri Shortcuts | 无 `AppShortcutsProvider`（Spotlight 的 App Intents 已有） |
| 搜索 | 结构化搜索页（字段组合过滤） | Search Center 筛选抽屉部分覆盖，原条目未单独立项 |
| 搜索 | 保存搜索 | 仅 v1 建表预留 `saved_searches`（含注释「Week 4+ 会扩展」），无功能 UI |
| P1 AI 老条目 | 同义标签检测、14 预设分类、支持平台识别 | 未做 |
| P1 AI 老条目 | 自然语言查询、命中原因说明 | 语义搜索 / RAG 部分覆盖语义，原条目未勾 |
| P1 AI 老条目 | 批量未分类整理 | 实质已有等价实现（BatchAI 整理工作台 + 自动后台整理），条目未勾、口径过时 |
| P2 高级 AI | 调研报告生成、对比报告 | 未做 |
| P2 效率 | Pin 置顶窗口、最近查看历史、常用标签快速访问 | 未做（仓库置顶 Pin 是另一回事，已做） |
| P2 订阅 | BYOK 模式 | 架构事实已满足（AI 全 BYOK），条目口径过时 |
| 非功能 | 启动时间 / 滚动 FPS / 搜索响应 / AI 摘要耗时 4 项指标 | 未逐项量化验收（遥测底座已有） |
| 工程债 | D-06 / D-07 / D-08 / D-09 / D-10 / D-11 / D-12 / D-15 / D-17 / D-23 | 10 项小债未还 |

**部分完成（⏸，总览已如实登记）**：知识库 RAG 整体交付（金标评测 + UI 人工验收待做）、RAG 收尾稳定性（同上）、公开分享链接与 OG 预览（生产部署 + 跨平台验收待做）、Agent 外部 Runtime（基本可用，持续打磨）。

---

## 6. 总览文档自身的健康问题（建议清理项）

1. **仪表盘失真**：自报 P0 74/74、P1 52/55、P2 39/64、合计 165/193（85%）。按 §3 的 A1–A19 补登后，实际完成数与比例都会明显上调（尤其 P2：Widget、Liquid Glass、独立 README 窗、Alfred/Raycast/uTools 均在 P2/原 P0-P2 未勾池里）。
2. **重复脏条目**：1438 行存在一条未勾选的「AI 语义搜索下拉 Pro 门控」，与 1439 行已勾选的同一功能重复。
3. **勾选指向已删除代码**：「Starcat 精选发布台」登记为 2026-08-13 完成（`Starcat/Features/CuratedPublisher`），但该目录已于 **2026-09-06 被 `1699e097`「移除 CuratedPublisher 运营工具及其相关测试」整体删除**。其能力疑似由 Awesome 来源管理（`AwesomeSourceManagerSheet`）+ 后端运营台承接，去向需确认后改写该条目。
4. **上架准备清单过时**：截图、隐私政策 HTTPS、描述文案、截图标语、TestFlight、Sandbox 合规检查 6 项未勾，但 App 已发布 9 个 tag / 7 个大版本并上架 Mac App Store。
5. **「下一步行动」节停在 2026-06-17**：表中 #4 StoreKit 拍板、#5 双渠道支付等实际已完成，整节与当前节奏脱节。
6. **测试数字基线停在 2026-08-18（2288 项）**：1.7.0 期间新增 LocalAI / Anthropic / 翻译 / Widget 等多轮测试未计入。
7. **双轨登记失控**：`docs/4-工程进度/` 专项目录（AI用量 / AI个人笔记 / Anthropic与CCSwitch / Awesome发现 / macOS桌面小组件等）与总览 checkbox 各自演进，1.6.0 之后新功能普遍只走专项目录 + changelog，不再进 checkbox。建议明确「专项归档 + 总览登记」的强制顺序。
8. **R-10 状态需复核**：总览注明「未部署 / 未迁库」，但 `supports/starcat-api`（聚合网关）与 `starcat-api-kit` 仓库均已建（最后提交 2026-08-31），实际部署进度待确认。

---

## 7. 质量快照

- **主项目测试**：333 个测试文件；按域聚簇：Agent 21、RAG 13、Readme 20、Repo/Repository 30、GitHub 24、**LocalAI 15**、Widget 8、AI 11、Awesome 5、Smart 7、Ambient 6、Companion 7、Release 6、Search 7、RepoContextPacker 9 等。
- **后端独立基线**（不计入主项目）：weekly-api 32 / trending-api 82 / sharing-api 33 / wiki-api —（总览口径）。
- **代码卫生**：Features 506 文件仅 1 处 TODO；Core 无 FIXME 命中；无空壳 View、无被注释掉的入口。
- **Release 无入口模块**：仅 Ambient（有意 DEBUG-only）。

---

## 8. supports 生态快照（29 项）

| 类别 | 仓库 | 状态 |
|---|---|---|
| 后端 API（Go，12） | starcat-api（聚合网关）、starcat-api-kit、weekly(534 文件)、trending、discovery、wiki、sharing、license、recommend、collection、history-api(Python+Go, 1443 文件) | 多数最后提交 2026-08-31；**history-api 2026-09-15 仍活跃** |
| 数据服务（Python） | starcat-recsys-trainer（9641 文件，含依赖） | 2026-09-05 |
| 运维 | starcat-admin-console（TS/React，215 文件）、supports/scripts（fly 备份 / secrets / history daily sync） | — |
| 站点与内容 | starcat-site（2026-09-16 活跃）、starcat-docs（同日活跃）、starcat-pro、starcat-localization | — |
| 客户端配套 | starcat-cli（Go）、starcat-skill、extensions/chrome、extensions/safari | — |
| 外部启动器 | starcat-alfred-workflow（Go）、starcat-raycast-extension（TS）、starcat-utools-plugin（JS） | 已发布配套 |
| 分发 | homebrew-starcat（2026-09-07）、homebrew-starcat-cli | — |

---

## 9. 建议的后续动作（待 dong4j 拍板，本文档不代执行）

1. **集中补一轮 1.6.0–1.7.0 的总览登记**：将 §3 的 A1–A19 逐条补 `- [x]` + `> 实现：` 行（遵守 ≤200 字 / 一段话规范），重算进度仪表盘与测试基线。
2. **清理脏数据**：删除 1438 行重复条目；改写「精选发布台」条目说明代码已移除及能力去向；上架准备清单按「已上架」收口；刷新「下一步行动」节。
3. **backlog 归位**：§4 的 B1–B12 建议统一挂到 v2 规划池（`v2-功能规划.md`）或总览对应章节，标明「方案就绪 / 待拍板」，避免散落在需求讨论目录中失联。
4. **建立「专项完成 → 总览登记」的收尾门禁**：4-工程进度专项关闭时强制回写总览 checkbox，防止再次出现双轨漂移。
5. **复核两个待确认状态**：R-10 聚合网关部署进度；精选发布台能力承接（Awesome 来源管理 / admin-console）。

---

## 10. 执行记录（2026-09-16 02:06，dong4j 授权「开干」后）

§9 建议已全部执行：

1. **总览补登（§3 A1–A19）**：新增 / 勾选 20 条 checkbox（含 Liquid Glass、独立 README 窗、Widget 三处原未勾条目），均带 `> 实现：` 行；仪表盘按全量 checkbox 重算为 **404 项 / 364 完成（90.1%）**，P0 155/145、P1 105/98、P2 144/121；阶段路线 Post-MVP 同步为 121/144。
2. **脏数据清理**：删除 1478 行未勾选的重复「AI 语义搜索下拉 Pro 门控」；精选发布台条目追加移除说明（`1699e097`，2026-09-06，运营回归后端管理接口）；上架准备清单 6 项按 v1.0.0 上架（2026-07-10）收口；「下一步行动」整节刷新为 12 行新表（含方案池拍板、RAG 金标、R-10 切流）。
3. **backlog 归位**：`v2-功能规划.md` 新增 §6「方案就绪待拍板」（B1–B12 归位，原变更日志顺延为 §7）。
4. **收尾门禁**：`AGENTS.md`「主进度索引」新增第 5 条——专项关闭必须同批回写总览 checkbox（写入前仍需 dong4j 确认）；总览维护脚注同步。
5. **两个待确认状态的结论**：R-10 按专项完成结果报告口径更新（Fly 首轮五库种子迁移与验证完成，重回维护模式，最终同步 / 生产复验待发布窗口），已在 R-10 条目追加「补充（2026-09-16 复盘）」行；精选发布台确认无显式承接方，总览按事实登记移除与去向（后端管理接口），不做能力承接推测。
6. 变更日志登记一条：`2026-09-16 02:06: 复盘补登 1.6.0-1.7.0 功能条目、清理重复与过时勾选、重算仪表盘`。

未执行：git 提交（未获授权）；测试全量运行刷新 2288 运行基线（需关 Xcode 后 `make test`，建议下次开发前执行）。

---

*复盘方法说明：本清单以代码为单一事实来源（模块目录、迁移账本、project.yml target、测试文件、supports 仓库、git 历史），文档仅作对照。凡「🧩」条目均给出可点击核验的代码路径；凡「B 类」条目均经代码检索确认无实现。*

*下一期复盘建议关注：B1–B3 三个待开干方案的落地情况、RAG 金标评测收口、CloudKit 是否启动。*
