# 第一轮审查报告 — 架构与协议边界

> 日期：2026-09-14
> 分支：`feature/anthropic-cc-switch-import`
> 范围：文档 68 Anthropic Messages API + 文档 69 CC Switch 导入

## 结论

实现已按冻结设计落到独立 worktree，未占用 `dev` checkout。目标单测 33 项中，流式 SSE 曾因空行被 `URLSession.bytes.lines` 吞掉而把两帧 JSON 拼在一起解析失败；已改为「新 `event:` 先冲刷上一帧」，`AnthropicClientTests` 7/7 通过。

## 已核对

- `AIServiceProvider.anthropic` 追加在 `localAI` 之后；`supportsEmbeddingEndpoint = false`。
- `AIClientFactory` 对 `.anthropic` 构造 `AnthropicClient`，不再误送 `OpenAIClient`。
- Endpoint 保留 `/anthropic` path；DeepSeek 中转测例断言 `.../anthropic/v1/messages`。
- 连续 `.tool` 历史合并为一条 Anthropic `user` + 多个 `tool_result`。
- `/models` 401 不回落目录；404 回落 bundled 并 ping `/messages`。
- CC Switch mapper：`anthropicAvailable` true 保留 `/anthropic`；false 改写 `.deepSeek` 默认 URL。
- Catalog `git diff --stat` 仅 +323 行，无删除。

## 本轮发现

| ID | 严重度 | 项 | 处理 |
|---|---|---|---|
| R1-1 | P0 | SSE 空行丢失导致 `JSONSerialization` garbage at end | 已修 |
| R1-2 | P2 | `listModels` 的 ping 会记一条 chat 用量 | 暂留：设计要求 ping 验 Key；用量是旁路，不挡导入 |
| R1-3 | P2 | 导入进度 `N / M` 未走 i18n | 下一轮 UI/i18n 审查处理 |

## 未在本轮手测

设置页新增 Anthropic、Direct 读 `~/.cc-switch/cc-switch.db` 仍需 dong4j 在真机点一次。本轮只跑命令行单测。
